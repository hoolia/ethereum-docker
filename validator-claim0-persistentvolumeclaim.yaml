apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    labels:
        io.kompose.service: validator-claim0
    name: validator-claim0
spec:
    accessModes:
        - ReadOnlyMany
    resources:
        requests:
            storage: 100Mi
