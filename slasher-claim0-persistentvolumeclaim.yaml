apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    labels:
        io.kompose.service: slasher-claim0
    name: slasher-claim0
spec:
    accessModes:
        - ReadOnlyMany
    resources:
        requests:
            storage: 100Mi
